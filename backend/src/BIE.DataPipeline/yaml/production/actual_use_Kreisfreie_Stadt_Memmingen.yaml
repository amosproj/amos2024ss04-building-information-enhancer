# describe the source
source:
  # link | filepath
  type: URL
  location: https://download1.bayernwolke.de/a/tn/lkr/tn_09764.zip
  data_format: SHAPE
options:
  # skip lines at the beginning
  skip_lines: 0
  # discard any rows that have null values
  discard_null_rows: false
  # how to deal with existing table. Options: ignore, replace, skip (default).
  if_table_exists: skip
table_name: actual_use_Kreisfreie_Stadt_Memmingen

table_cols:

dataset: actual_use
